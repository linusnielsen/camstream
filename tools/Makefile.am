bin_PROGRAMS = gst-camstream

# sources used to compile this plug-in
gst_camstream_SOURCES = gstcamstream.c

# compiler and linker flags used to compile the program, set in configure.ac
gst_camstream_CFLAGS = $(GST_CFLAGS)
gst_camstream_LDADD = $(GST_LIBS)

